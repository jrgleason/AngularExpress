extends layout

block content
  h1= title
  p Welcome to #{title}
  .angular(ng-app="phonecatApp")
    .controller(ng-controller="PhoneListCtrl")
      ul
        li(ng-repeat="phone in phones"){{phone.name}}
          p {{phone.snippet}}
